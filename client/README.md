# KinderKompas — client (React + TypeScript)

Placeholder voor de frontend-SPA. Wordt opgezet zodra de eerste API-endpoints met
autorisatie staan (fase 3+).

Beoogde stack: React + TypeScript via Vite, die de KinderKompas Web API consumeert.

Opzetten (later):

```bash
npm create vite@latest . -- --template react-ts
npm install
npm run dev
```
